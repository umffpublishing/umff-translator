# 울산울주세계산악영화제(UMFF) 출판물 번역기

UMFF 2026 프로그램북 콘텐츠(시놉시스·프로그램 노트·바이오 등)의 **한↔영 초벌 번역** 웹 도구.
앞서 만든 감수 도구(umff-review)와 동일한 아키텍처를 쓴다: 정적 단일 페이지 + 브라우저에서
Anthropic API 직접 호출 + GitHub Pages 배포.

---

## 1. 아키텍처 & 파일 구조

빌드 단계 없음. 순수 HTML/CSS/JS 단일 페이지.

```
index.html              앱 본체 (UI + 글로서리 로더 + 프롬프트 빌더 + API 호출 + 스트림 파서)
report.html             결과 리포트 뷰 (선택)
glossary/
  proper-nouns.csv      고정 명칭 한↔영 대조표 (구분,국문,영문,비고)
  festivals-schools.csv 해외 영화제·학교 (본문에 등장할 때만 사용)
  notation-rules.md     표기 규칙 (문장형)
  style-guide.md        번역 문체 가이드 (규칙 1~22, 길이 기준) — 번역기에서 추가됨
  tm-examples.md        유형×방향별 few-shot 예시 (확정 원고에서 선별) — 시스템 프롬프트에 주입
  translation-memory.csv  2025 확정 원고 97편 원본 코퍼스 (전체 주입 X · tm-examples.md의 출처)
CLAUDE.md               이 파일
```

- 글로서리는 `fetch('./glossary/...')`로 런타임 로드한다. 실패 시(예: `file://`로 열었을 때)
  `index.html`에 내장된 폴백(`EMBEDDED_CSV` / `EMBEDDED_RULES`)으로 동작한다.
- **글로서리 파일을 수정하면 `index.html`의 내장 폴백도 함께 갱신**해야 둘이 어긋나지 않는다.

---

## 2. 실행 / 배포

```bash
# 로컬 개발 — 반드시 서버로 띄운다 (file://는 글로서리 fetch가 막혀 폴백으로만 동작)
python3 -m http.server 8000      # http://localhost:8000

# 배포 — main 브랜치 푸시 → GitHub Pages 자동 반영
```

---

## 3. 번역 역할 & 핵심 원칙

UMFF 출판물 한↔영 **초벌 번역자**. 의미를 정확히 옮기고 자연스러운 문장을 만들되,
최종 확정은 담당자/원어민 감수의 몫이다.

### 출처 우선순위 (충돌 시 위에서부터)
1. **proper-nouns.csv / festivals-schools.csv** — 고정 명칭의 확정 표기
2. **notation-rules.md** — 표기 규칙
3. **style-guide.md** — 문장 구조·문체·번역 방향

> `translation-memory.csv`는 위 권위 출처가 아니라 **문체·길이 참고 코퍼스**다.
> 그 안의 고정 명칭은 이미 글로서리에 반영돼 있고, 문장쌍은 `tm-examples.md`를 통해 few-shot으로만 쓴다.

### 절대 원칙
- **고정 명칭은 룩업 먼저.** 고유명사·감독명·작품명·섹션·시상·장소·영화제·학교가 나오면
  글로서리에서 먼저 찾고, 있으면 **그 표기를 그대로** 쓴다. 더 자연스러워 보여도 바꾸지 않는다.
- **글로서리에 없으면 추측하지 않는다.** `[확인필요: 원문]`으로 표시한다 ("틀림"이 아니라 "미확정").
- 국문 하나에 영문 후보가 여럿일 때(예: `개막작 / 폐막작 = Opening Film / Closing Film`)는
  문맥에 맞는 것을 고르고, 애매하면 `[확인필요]`.
- **의미·정보를 임의로 더하거나 빼지 않는다.** 길이 기준 때문에 내용을 누락시키지 않는다.
- 사람 이름이 본인 선호 영문명을 가진 경우 표준 로마자보다 우선한다(예: 이순걸 → Lee Soon-geol).

---

## 4. 콘텐츠 유형별 번역 방향 (style-guide.md 요약)

세 유형은 방향(한→영 / 영→한)에 따라 규칙이 다르다. 상세는 `glossary/style-guide.md` 참조.

**시놉시스**
- 영→한: 관계절·분사구를 앞당긴 관형절로, 영어 단문을 연결어미로 잇고, 시제는 현재형 유지.
- 한→영: 배경 맥락은 문두 부사구로, 시간 전환은 전환사(`Years later`, `Meanwhile` 등), historical present 유지.

**프로그램 노트**
- 1인칭 노트(감독 작성)는 양방향 모두 1인칭 유지. 3인칭 노트는 반복 주어를 한국어에서 생략.
- 한→영 3인칭 노트는 보통 `This film` / `The film`으로 시작. 한국어 긴 복문은 영어에서 2~3문장으로 분리.

**바이오**
- 영→한: 이름 생략하고 `[국가] 출신의 [직함]`으로 시작, 경력은 현재형.
- 한→영: 감독 이름을 주어로 시작, 경력·수상은 현재완료(`has won` 등).

### few-shot 예시 (glossary/tm-examples.md)
- `translation-memory.csv`(2025 확정 원고 97편)에서 **유형×방향별 대표 예시 2~3개씩**을 골라
  `tm-examples.md`에 정리하고, 시스템 프롬프트에 함께 주입한다.
- 전체 코퍼스(약 280KB)를 매 호출 주입하지 않는다 — 선별 예시만으로 문체·길이 감각을 잡고 토큰을 아낀다.
- 특정 유형(특히 노트·바이오) 품질이 부족하면 그 유형의 예시 수를 늘린다.
- 예시도 캐시되는 시스템 프롬프트의 일부이므로 자주 바꾸지 않는다(캐시 적중률 유지).

---

## 5. 길이 기준 (공백 포함, 조직위 공식 스펙)

| 유형 | 국문 | 영문 글자 | 영문 단어 |
|---|---|---|---|
| 시놉시스 | 215자 | 390자 | 70단어 |
| 프로그램 노트 | 150자 | 260자 | 35단어 |
| 바이오 | 110자 | 260자 | 50단어 |

목표 상한이다. 원문 정보량이 많아 한도를 넘기면 **누락보다 초과를 우선 허용**하고 감수에서 조정한다.
짧은 원문을 한도까지 늘려 채우지 않는다.

---

## 6. 표기 규칙 (notation-rules.md — 반드시 적용)

전체는 `glossary/notation-rules.md`에 있다. 번역 시 자주 걸리는 항목:
- 작품 제목: 국문 꺾쇠 `< >`, **영문은 이탤릭**. 국영문 병기는 꺾쇠 하나 `<한글 English>`.
- 인용/대사: 국·영 모두 큰따옴표 `" "`. **의역 인용·강조어에만** 작은따옴표 `' '`.
  (즉 "영어 큰따옴표 → 한국어 작은따옴표" 일괄 변환은 틀림.)
- 말줄임표: 아래 점 3개 `...` 만. (`···`·`…` 금지)
- 콜론·세미콜론: 앞 글자에 붙이고 뒤는 한 칸.
- 작품 정보: `국가 | 연도 | 러닝타임(53min, 마침표X) | Color/B&W | 카테고리 | 등급` / 연도는 최종 완료 시점만.
- 별명·애칭: 첫 언급 시 작은따옴표, 이후 부호 없음.
- 도서 『 』 / 논문·간행물·TV 「 」 / 전시 《 》 / 음반 「 」+노래 `' '` / 공연 국문 `' '`·영문 이탤릭.
- 날짜·시간: 24시간 4자리, AM/PM 미사용, 범위는 ` - `(앞뒤 한 칸). 월 약어는 글로서리 `월` 항목.

---

## 7. 글로서리

CSV 형식: `구분,국문,영문,비고` (UTF-8로 저장).

- `proper-nouns.csv` 구분값: 영화제명 · 인명 · 장소 · 섹션 · 시상 · 프로그램 · 용어 · 국가 · 등급 · 자막 · 월 · 가격 · 주소 · 전화번호.
- `festivals-schools.csv` 구분값: 해외영화제 · 해외학교. **본문에 해당 명칭이 나올 때만** 시스템 프롬프트에 함께 주입한다(평소엔 토큰 절약).
- 비고에 `(원본 그대로)` 등 메모가 있으면 그 지시를 따른다. 원본 표기 흔들림(오타 의심: `Festvial`, `Edinburg`, `Live Dud`, `May.` 등)은 임의로 고치지 말고 글로서리 값을 그대로 쓴다 — 수정은 조직위 확정 후.

---

## 8. 출력 형식 (시스템 프롬프트 ↔ 파서 계약)

모델 출력은 아래 구분자를 **그대로** 사용한다. `parseResponse()`가 이 구분자로 섹션을 추출한다.

```
[SECTION:번역문]
번역 결과 전문. 고정 명칭은 글로서리 표기 그대로, 미확정 고유명사는 [확인필요: 원문] 인라인 표시.
[/SECTION]

[SECTION:확인필요]
유형별 나열 — 작품명: X / 인명: Y / 표기: Z  (없으면 "확인 필요 항목 없음")
[/SECTION]

[SECTION:적용]
글로서리에서 적용한 고정 명칭과, 판단이 필요했던 규칙을 간단히. (없으면 생략)
[/SECTION]

[SECTION:길이]
유형 / 국문 N자 / 영문 N자 · N단어 / 한도 대비 (초과 시 표시)
[/SECTION]
```

번역기는 **방향(한→영·영→한)**과 **유형(시놉시스·노트·바이오)**을 입력에서 선택받아 시스템 프롬프트에 명시한다.

---

## 9. 코딩 규칙 & 주의사항

**API 호출** (감수 도구와 동일 패턴)
- 엔드포인트: `POST https://api.anthropic.com/v1/messages`, 스트리밍(`stream: true`).
- 헤더: `x-api-key`, `anthropic-version: 2023-06-01`,
  `anthropic-beta: prompt-caching-2024-07-31`, `anthropic-dangerous-direct-browser-access: true`.
- 시스템 프롬프트는 글로서리+규칙+few-shot 예시(tm-examples.md)를 주입하고 `cache_control: { type: 'ephemeral' }`로 캐싱한다(내용이 크고 매 호출 재사용되므로 비용·지연 절감).
- 모델: 번역은 품질 우선이므로 **`claude-sonnet-4-6`**와 **`claude-opus-4-8`** 두 개만 둔다(Haiku는 번역엔 부적합, 제외).
  - 초기에는 두 모델을 **토글로 둘 다 선택 가능**하게 해, 까다로운 원고(고유명사 多·모호·장문)로 A/B 비교 후 하나로 굳힌다.
  - 기본값은 `claude-sonnet-4-6`. 최종 인쇄 직전 검수 등 품질이 최우선일 때 Opus로 전환.
  - 표준 요금(100만 토큰): Sonnet 4.6 $3/$15, Opus 4.8 $5/$25 (입·출력 약 1.67배 차). 프롬프트 캐싱으로 큰 시스템 프롬프트 비용은 대부분 상쇄된다.
  - 모델 ID·가격은 변동되므로 docs.claude.com에서 확인 후 `MODEL_CONFIG` 갱신.
- `max_tokens`는 번역문+부가 섹션을 담을 만큼 충분히(감수 도구는 8000). 긴 입력은 잘리지 않게 여유 있게.

**보안**
- API 키는 `localStorage('umff_api_key')`에만 저장. **코드·저장소에 키를 하드코딩하거나 커밋하지 않는다.**
- 브라우저 직접 호출이므로 키는 사용자 본인 것이며 클라이언트에 노출된다. README에 그 점을 안내한다.

**일관성**
- 글로서리 CSV/MD를 고치면 `index.html` 내장 폴백(`EMBEDDED_*`)도 같이 갱신한다.
- 출력 `[SECTION:...]` 구분자를 바꾸면 `buildSystemPrompt()`와 `parseResponse()`를 동시에 수정한다.
- CSV를 코드로 읽을 일이 있으면 UTF-8 BOM 대비해 `utf-8-sig`로 읽는다(원본 TM CSV에 BOM이 있었음).

**하지 말 것**
- 고정 명칭을 글로서리 조회 없이 임의 번역.
- 길이 한도를 맞추려 원문 내용 누락.
- 글로서리 오타를 임의 교정(조직위 확정 전).
